
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    function isSignedIn() {
      return request.auth != null;
    }

    function isOwner(userId) {
      return isSignedIn() && request.auth.uid == userId;
    }
    
    function isAdmin() {
      return exists(/databases/$(database)/documents/roles_admin/$(request.auth.uid));
    }
    
    function isOrdersManager() {
      return exists(/databases/$(database)/documents/roles_orders_manager/$(request.auth.uid));
    }

    function isFinanceManager() {
      return exists(/databases/$(database)/documents/roles_finance_manager/$(request.auth.uid));
    }
    
    function isMerchant() {
        return exists(/databases/$(database)/documents/roles_merchant/$(request.auth.uid));
    }

    function isStaff() {
      return isAdmin() || isOrdersManager() || isFinanceManager();
    }

    // Default-deny all reads and writes
    match /{document=**} {
      allow read, write: if false;
    }

    // User profiles
    // A user can always read their own profile. Staff can read any profile.
    match /users/{userId} {
      allow create: if isOwner(userId);
      allow get: if isOwner(userId) || isStaff();
      allow update: if isOwner(userId) || isAdmin();
      allow list, delete: if isAdmin();
    }
    
    // Wallets
    match /wallets/{userId} {
        allow get: if isOwner(userId) || isFinanceManager();
        allow create, update: if false; // Managed by server-side logic
    }

    // Role documents for permission checks.
    match /roles_admin/{userId} { 
        allow get: if isOwner(userId);
        allow write, list, delete: if isAdmin();
     }
    match /roles_orders_manager/{userId} {
        allow get: if isOwner(userId);
        allow write, list, delete: if isAdmin();
    }
    match /roles_finance_manager/{userId} {
        allow get: if isOwner(userId);
        allow write, list, delete: if isAdmin();
    }
    match /roles_merchant/{userId} {
        allow get: if isOwner(userId);
        allow write, list, delete: if isAdmin();
    }
    
    // Public Settings
    match /publicSettings/{docId} {
        allow get: if true;
        allow write: if isAdmin();
    }

    // Products and Categories
    match /products/{productId} {
        allow read: if isSignedIn();
        allow create: if isAdmin() || isMerchant();
        // Merchant can only update their own products. Admin can update any.
        allow update: if isAdmin() || (isMerchant() && resource.data.merchantId == request.auth.uid);
        allow delete: if isAdmin() || (isMerchant() && resource.data.merchantId == request.auth.uid);
    }
    match /productCategories/{categoryId} {
        allow read: if isSignedIn();
        allow create, update, delete: if isAdmin();
    }
     // Anyone can track a product click via the public page
    match /products/{productId}/clicks/{clickId} {
        allow create: if true;
        allow read, list, update, delete: if false;
    }
    
    // Orders
    match /orders/{orderId} {
      allow create: if (isSignedIn() && request.resource.data.dropshipperId == request.auth.uid) || request.auth == null;
      allow list: if isSignedIn(); // Allow list queries for all signed-in users
      allow get: if isSignedIn() && (
        resource.data.dropshipperId == request.auth.uid || 
        isStaff() ||
        (isMerchant() && get(/databases/$(database)/documents/products/$(resource.data.productId)).data.merchantId == request.auth.uid)
      );
      // Order manager can update anything. Merchant can only update status.
      allow update: if isOrdersManager() || (isMerchant() && get(/databases/$(database)/documents/products/$(request.resource.data.productId)).data.merchantId == request.auth.uid);
      allow delete: if isAdmin();
    }

    // Withdrawal requests
    match /adminWithdrawalRequests/{reqId} {
        allow read, list, update, delete: if isFinanceManager();
        allow create: if false; // Only server can create
    }
    match /users/{userId}/withdrawalRequests/{reqId} {
        allow read, list: if isOwner(userId);
        allow create, update, delete: if false; // Managed by server-side logic
    }
    
    // Merchant Inquiries can be created by anyone.
    match /merchantInquiries/{inquiryId} {
        allow create: if true;
        allow read, list, update, delete: if isAdmin();
    }
    
     // Marketing customers can be created by anyone who successfully refers them
    match /marketingCustomers/{contactId} {
      allow create, update: if true;
      allow read, list, delete: if isAdmin();
    }

    // Stock Ledger
    match /stockLedger/{ledgerId} {
        allow read, list: if isAdmin();
        allow create: if isAdmin() || isMerchant();
        allow update, delete: if false;
    }

    // Helper collections
    match /id_generator/{docId} {
        allow create: if isSignedIn();
    }
    match /webhooks_bosta/{webhookId} {
        allow create: if true; // Webhooks can write
        allow read, list, update, delete: if isAdmin();
    }
  }
}
